name: iCal Sync

on:
  schedule:
    # Every 30 minutes (GitHub may delay by 5-15 min under load)
    - cron: '*/30 * * * *'
  workflow_dispatch:
    # Allow manual trigger from GitHub UI

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call sync endpoint
        run: |
          echo "Triggering iCal sync at $(date)"
          response=$(curl -s -w "\n%{http_code}" https://villasolria.com/api/ical/sync)
          body=$(echo "$response" | head -n -1)
          status=$(echo "$response" | tail -n 1)

          echo "Status: $status"
          echo "Response: $body"

          if [ "$status" != "200" ]; then
            echo "::error::Sync failed with status $status"
            exit 1
          fi

          # Check if sync was successful
          if echo "$body" | grep -q '"success":true'; then
            echo "::notice::iCal sync completed successfully"
          else
            echo "::error::Sync returned success:false"
            exit 1
          fi
